@page
@model ProductsIndexModel
@{
    ViewData["Title"] = "Products";
}

/* NEW FILE - Products list (static demo)
   - This is a minimal page so the /Products path exists and renders.
   - Later: replace the static list with your repository/service calls.
*/

<h2>Products (Demo)</h2>

<p>This demo page shows a static list so you can confirm Products route works.</p>

<ul class="list-group">
    <li class="list-group-item">Notebook — ₱50 — <span class="text-muted">demo only</span></li>
    <li class="list-group-item">Pen — ₱10 — <span class="text-muted">demo only</span></li>
</ul>
